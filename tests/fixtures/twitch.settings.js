// Sanitized capture of the primary settings contract. Whitespace and JSON
// escaping are semantically harmless and must not require a fallback parser.
window.__settings = {
  "spade_url"  :  "https:\/\/spade.example\/submit"
};
